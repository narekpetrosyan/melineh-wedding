generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model Guest {
  id    Int     @id @default(autoincrement())
  name  String
  count Int
  wish  String?

  @@map("guests")
}
